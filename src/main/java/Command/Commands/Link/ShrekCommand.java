package Command.Commands.Link;

import Command.Structure.LinkCommand;

public class ShrekCommand extends LinkCommand {
    public ShrekCommand(String json) {
        super("shrek", "Shrek having a good time!", json);
    }
}
