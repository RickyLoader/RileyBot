package Command.Commands.Link;

import Command.Structure.LinkCommand;

public class UnderbellyCommand extends LinkCommand {
    public UnderbellyCommand(String json) {
        super("underbelly", "A map of the underbelly!", json);
    }
}
